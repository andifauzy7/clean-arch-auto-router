name: dashboard_presentation
description: "dashboard presentation layer"
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  common:
    path: ../../../shared/common
  auth_presentation:
    path: ../../../features/auth/auth_presentation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  auto_route_generator:
  build_runner:
flutter: